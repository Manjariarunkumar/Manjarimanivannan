PROGRAM:
#include <stdio.h>
int max();
int main()
{
  max();
}
int max()
{
   int i,a[4]={1,2,3,4};
   int max_v=a[0];
   for(i=1;i<4;i++)
   if(a[i]>max_v)
   {
     max_v=a[i];
   }
     printf("The max value of array is %d",max_v);
}
OUTPUT:
The max value of array is 4
    
 
