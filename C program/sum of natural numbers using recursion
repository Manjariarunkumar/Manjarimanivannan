PROGRAM:
#include <stdio.h>
int main()
{
   int n;
   printf("enter the number:");
   scanf("%d",&n);
   printf("the sum of %d is %d",n,sum(n));
}
int sum(int n)
{
    if(n==0)
    return 0;
    else
    return n+sum(n-1);
}

OUTPUT:
enter the number:5
the sum of n is 15
